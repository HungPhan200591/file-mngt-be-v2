# Feature documents

Mỗi feature tạo một folder theo dạng `NNN-short-slug`, dùng ba template trong `docs/templates/`.

Agent triển khai đọc `03-plan.md` của feature đang làm trước, context owner và contract trực tiếp liên quan. Chỉ mở Brief/Design khi Plan chỉ đến; không đọc toàn bộ feature lịch sử.
