# Feature documents

Mỗi feature tạo một folder theo dạng `NNN-short-slug`, dùng ba template trong `docs/templates/`.

Agent triển khai chỉ đọc feature folder đang làm, context owner và contract trực tiếp liên quan; không đọc toàn bộ feature lịch sử.
