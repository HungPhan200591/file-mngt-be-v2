# Event contracts module

Module này sau khi bootstrap chỉ chứa event envelope và DTO contract versioned. Không đặt entity, repository, service nghiệp vụ hoặc utility chung không liên quan contract vào đây.

Tài liệu nguồn chuẩn vẫn là `docs/contracts/events/`; Java contract phải khớp tài liệu đó.
