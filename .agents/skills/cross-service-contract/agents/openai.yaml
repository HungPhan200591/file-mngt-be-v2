interface:
  display_name: "Cross-service Contract"
  short_description: "Protect Backend V2 service contracts"
  default_prompt: "Use $cross-service-contract to design or change this REST, Kafka, database, or cross-service contract safely."
