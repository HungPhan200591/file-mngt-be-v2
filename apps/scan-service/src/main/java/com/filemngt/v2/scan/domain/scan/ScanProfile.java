package com.filemngt.v2.scan.domain.scan;

/** Profile xác định nguồn filesystem và quy ước parser áp dụng cho một scan root. */
public enum ScanProfile {
    JOKE_VIDEO,
    JOKE_ASSET,
    USE_VIDEO,
    USE_ASSET,
    USE_ALBUM
}
