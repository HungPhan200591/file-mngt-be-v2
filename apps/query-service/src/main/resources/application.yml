spring:
  application:
    name: query-service
  profiles:
    default: local

server:
  port: ${QUERY_SERVER_PORT:18103}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
