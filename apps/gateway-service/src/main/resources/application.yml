spring:
  application:
    name: gateway-service

server:
  port: ${GATEWAY_SERVER_PORT:18100}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      probes:
        enabled: true
      show-details: never
