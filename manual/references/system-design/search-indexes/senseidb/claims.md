# Claims

- Read model có thể đánh đổi write complexity để tối ưu query.
- Ordered/versioned stream giúp projection áp dụng update đúng thứ tự.
- Bootstrap batch và streaming update là hai phase bổ trợ.
- Denormalization loại bỏ join ở query hot path.
